{
  "name": "goat-recommendation-engine",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/tests/recommendationEngine.spec.js",
    "demo": "npm run build && node dist/examples/demo.js",
    "demo:photo": "npm run build && node dist/examples/photoAnalysisToRecommendation.demo.js",
    "demo:final": "npm run build && node dist/examples/finalServiceFlow.demo.js",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true});require('fs').rmSync('logs',{recursive:true,force:true});require('fs').rmSync('tmp',{recursive:true,force:true})\""
  },
  "devDependencies": {}
}
