import { useRouter } from "expo-router";
import React from "react";
import { ImageBackground, StyleSheet, Text, View } from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { BrandIcon } from "@/src/components/BrandIcon";
import { GoatMark } from "@/src/components/editorial/Brand";
import { PrimaryButton } from "@/src/components/editorial/UI";
import { useApp } from "@/src/context/AppContext";
import { editorialImages } from "@/src/data/editorialContent";
import { fonts, palette } from "@/src/theme/editorial";
export default function NoResultsScreen() { const router = useRouter(); const insets = useSafeAreaInsets(); const { recommendationMethod, pendingAttempt } = useApp(); const selectionRoute = (pendingAttempt?.initialSelection.method ?? recommendationMethod) === "reference" ? "/reference-selection" : "/mood-selection"; return <ImageBackground source={editorialImages.harbor} style={styles.screen} imageStyle={styles.background}><View style={styles.wash} /><View style={[styles.content, { paddingTop: insets.top + 18, paddingBottom: insets.bottom + 22 }]}><GoatMark /><View style={styles.center}><View style={styles.rings}><View style={styles.ring2}><BrandIcon name="map" size={41} color={palette.forest} /></View></View><Text style={styles.code}>NO PLACE FOUND</Text><Text style={styles.title}>조건에 딱 맞는 장소를{`\n`}찾지 못했어요</Text><Text style={styles.body}>조건을 조금 넓혀 다시 추천하거나,{`\n`}처음부터 새로운 장면을 찾아볼 수 있어요.</Text></View><View style={styles.actions}><PrimaryButton label="조건 다시 선택하기" onPress={() => router.replace("/travel-preference")} /><PrimaryButton label={selectionRoute === "/reference-selection" ? "장면 다시 고르기" : "감성부터 다시 찾기"} variant="outline" onPress={() => router.replace(selectionRoute as never)} /></View></View></ImageBackground>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: palette.ivory }, background: { opacity: .17 }, wash: { ...StyleSheet.absoluteFillObject, backgroundColor: "rgba(246,242,233,.89)" }, content: { flex: 1, paddingHorizontal: 22 }, center: { flex: 1, alignItems: "center", justifyContent: "center", marginTop: -20 }, rings: { width: 150, height: 150, borderRadius: 75, borderWidth: 1, borderColor: palette.line, alignItems: "center", justifyContent: "center" }, ring2: { width: 108, height: 108, borderRadius: 54, backgroundColor: "rgba(222,231,211,.95)", alignItems: "center", justifyContent: "center" }, code: { marginTop: 27, fontFamily: fonts.bold, fontSize: 9, letterSpacing: 1.8, color: palette.forestSoft }, title: { marginTop: 11, textAlign: "center", fontFamily: fonts.serif, fontSize: 29, lineHeight: 40, color: palette.ink }, body: { marginTop: 12, textAlign: "center", fontFamily: fonts.body, fontSize: 14, lineHeight: 23, color: palette.muted }, actions: { gap: 10 } });
