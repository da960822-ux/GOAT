import { Redirect } from "expo-router";

/** The discovery screen is the single scene/theme selection entry point. */
export default function MoodSelectionScreen() {
  return <Redirect href="/" />;
}
